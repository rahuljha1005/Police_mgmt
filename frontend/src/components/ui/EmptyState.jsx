const EmptyState = ({ message }) => (
  <div className="rounded-lg border border-dashed border-white/10 bg-white/[0.02] p-8 text-center text-sm text-zinc-400">
    {message}
  </div>
);

export default EmptyState;
